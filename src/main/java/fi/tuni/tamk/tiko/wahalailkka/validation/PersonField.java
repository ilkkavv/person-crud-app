package fi.tuni.tamk.tiko.wahalailkka.validation;

/**
 * Defines the available person form fields used in validation.
 */
public enum PersonField {
    /** Person identifier field. */
    ID,
    /** Person first name field. */
    FIRST_NAME,
    /** Person last name field. */
    LAST_NAME,
    /** Person age field. */
    AGE
}
